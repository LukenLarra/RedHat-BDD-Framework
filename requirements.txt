# BDD Framework Core Dependencies
# ==================================
# Este archivo contiene solo las dependencias necesarias para ejecutar
# el framework (bdd_framework.py). Las dependencias de cada servicio
# están en sus respectivos directorios.

PyYAML==6.0.1

# Instalación completa del proyecto:
# pip install -r requirements.txt              # Framework core
# pip install -r backend/requirements.txt      # Backend API
# pip install -r tests/requirements.txt        # Testing suite
# pip install -r requirements-dev.txt          # Development tools (pre-commit, formatters, linters)
# cd frontend && npm install                   # Frontend
# cd frontend && npm install --save-dev        # Frontend dev tools
